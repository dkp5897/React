import React from 'react'

// nested rote example
function FeaturedProdut() {
  return (
    <div>
      List od featured products
    </div>
  )
}

export default FeaturedProdut


// if we want that child component render as soon as we are clicking at it's parents componet then we are using index Route